<%= form_for(report) do |f| %>
  <% if report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(report.errors.count, "error") %> prohibited this report from being saved:</h2>

      <ul>
      <% report.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :제목 %>
    <%= f.text_field :title, id:"form_box_title" %>
  </div>

  <div class="field">
    <%= f.label :email %>
    <%= f.text_field :email, id:"form_box_email" %>
  </div>

  <div class="field">
    <%= f.label :내용 %>
    <%= f.text_area :content, id:"form_box_content" %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
